import { CommaToDotDirective } from './comma-to-dot.directive';

describe('CommaToDotDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaToDotDirective();
    expect(directive).toBeTruthy();
  });
});
